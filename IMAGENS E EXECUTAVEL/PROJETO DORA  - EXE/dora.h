#include <stdio.h>
#include <graphics.h>

//COMEÇO DA ESTRUTURA DA FUNÇÃO
//------------------------------------------------------------------------------------------------------------------------------------------------
int sobreEuropa()

{
                    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE EUROPA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int sobreOceania() //comando da função

{
    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE OCEANIA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int sobreAmerica()

{
    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE AMERICA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int sobreAntartida() //comando da função

{
    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE ANTARTIDA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int sobreAsia() //comando da função

{
    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE ASIA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int sobreAfrica() //comando da função

{
    setbkcolor(WHITE); // Define o fundo como branco
    setcolor(BLACK);   // Define a cor da escrita como preto

    outtextxy(20, 485, "SOBRE AFRICA"); // Exibe o texto na posição (20, 485)
}
//------------------------------------------------------------------------------------------------------------------------------------------------

int Continente() //comando da função
{
            setbkcolor(15); // Define o fundo como branco
            setcolor(0);   // Define a cor da escrita como preto
            outtextxy(30,830,"CONTINENTES"); // Exibe o texto na posição (20, 485)
}

